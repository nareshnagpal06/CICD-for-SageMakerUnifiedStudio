mlflow>=3.1,<4
sagemaker-mlflow==0.4.0
boto3
# IMPORTANT: Pin scikit-learn to the container's pre-installed version.
# The sagemaker-xgboost:1.7-1 container has scikit-learn==1.5.0 with
# compiled Cython extensions. Upgrading causes '__reduce_cython__' KeyError
# due to binary incompatibility. pip will warn about mlflow wanting >=1.3
# but the pin prevents the destructive upgrade. The training script only
# uses basic sklearn.metrics functions available in 0.24.1.
scikit-learn==1.5.0
